<% @scenes.each do |scene| %>
    <div class="card-scene">
      <div class="card-scene-infos">
        <h2><%= scene.title %></h2>
        <p>
          <%= link_to "Read More", scene, class: 'btn btn-dark' %>
        </p>
      </div>
    </div>
  <% end %>
  </div>
  <div class="card-create-btn">
    <%= link_to "Create scene", new_scene_path, class: 'btn btn-primary mb-5' %>
  </div>
